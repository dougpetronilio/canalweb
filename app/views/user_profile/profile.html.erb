nome: <%= @user.nome_completo %></br>
sexo: <%= @user.sexo %></br>
data de nascimento: <%= @user.nascimento %></br>
pais: <%= @user.pais %></br>
estado: <%= @user.estado %></br>
cidade: <%= @user.cidade %></br>
email: <%= @user.user.email %></br>

</br>
</br>
<% @canals.each do |canal| %>
  <table class='bordasimples'>
  <tr>
    <td><%= canal.nome %></td>
    <td><%= canal.tipo_programacao %></td>
    <td><%= canal.idade_minima %></td>
    <td><%= canal.imagem %></td>
    <td><%= canal.descricao %></td>
    <td><%= link_to 'Mostrar', canal %></td>
    <td><%= link_to 'Editar', edit_canal_path(canal) %></td>
    <td><%= link_to 'excluir', canal, confirm: 'tem certeza?', method: :delete %></td>
  </tr>
  </table>
<% end %>

<% unless Amigo.amizade?(current_user.user_profile, @user) %>
  <% if current_user.user_profile != @user %>
    <%= link_to "Adicionar amigo", :controller => "amigos", :action => "adicionar", :id => @user.id %>
  <% end %>
<% end %>
